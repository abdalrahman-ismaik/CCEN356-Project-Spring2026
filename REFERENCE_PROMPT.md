# CCEN356 Project — HTTP/HTTPS Performance & Visibility

> **Reference prompt for AI agent calls.** This file contains all project context, decisions, specs, and implementation details. Attach this file when asking an agent to write code, configs, or documentation for this project.

---

## Project Goal

Build a lab environment that **compares HTTP vs HTTPS performance** using physical Cisco networking equipment, Python automation scripts, Scapy packet capture, and data visualization. Deliverables: working lab setup, 5 Python scripts, Scapy capture logs, matplotlib charts, and a formal report.

---

## Environment

| Item | Detail |
|---|---|
| **Routers** | 2× Cisco 2901 (physical hardware) |
| **Switch** | 1× Cisco 2960 (physical hardware) |
| **Server PC** | Windows laptop, IP: 192.165.20.79/24, gateway: 192.165.20.37 |
| **Client PCs** | Windows laptops, IPs: 192.165.10.92, 192.165.10.79 (subnet /24, gateway: 192.165.10.37) |
| **Python** | 3.8+ on client PCs |
| **Team** | 2 members |

---

## Network Topology

```
[Client 1: 192.165.10.92] ──┐
[Client 2: 192.165.10.79] ──┤── [SW1 (2960)] ──── [R1 (2901)] ──Gi0/0── [R2 (2901)] ──Gi0/1── [Server: 192.165.20.79]
                            └── (SPAN port)
```

### IP Addressing Table

| Device | Interface | IP Address | Subnet Mask | Role |
|---|---|---|---|---|
| R1 | Gi0/0 | 10.1.5.21 | 255.255.255.252 | WAN link to R2 |
| R1 | Gi0/1 | 192.165.10.37 | 255.255.255.0 | LAN gateway (client side) |
| R2 | Gi0/0 | 10.1.5.22 | 255.255.255.252 | WAN link to R1 |
| R2 | Gi0/1 | 192.165.20.37 | 255.255.255.0 | LAN gateway (server side) |
| Server | NIC | 192.165.20.79 | 255.255.255.0 | Web server (Windows) |
| Client 1 | NIC | 192.165.10.92 | 255.255.255.0 | Test client |
| Client 2 | NIC | 192.165.10.79 | 255.255.255.0 | Test client |

### Routing

- R1: `ip route 192.165.20.0 255.255.255.0 10.1.5.22`
- R2: `ip route 192.165.10.0 255.255.255.0 10.1.5.21`

### Credentials

- Router enable secret: `cisco123`
- Router SSH user: `admin` / password: `admin123` (privilege 15)
- SSH: RSA 2048, version 2, domain `lab.local`, vty 0-4

---

## Directory Structure

```
CCEN356-Project-Spring2026/
├── scripts/
│   ├── ssh_connect.py            # Script 1: Netmiko SSH to routers
│   ├── capture_traffic.py        # Script 2: Scapy packet capture
│   ├── performance_metrics.py    # Script 3: HTTP vs HTTPS benchmarking
│   ├── visualize_traffic.py      # Script 4: Matplotlib charts
│   └── dashboard.py              # Script 5: Flask live dashboard
├── server/
│   ├── http_server.py            # Flask HTTP server (port 80)
│   ├── secured_server.py         # Flask HTTPS server (port 443)
│   ├── templates/
│   │   ├── index.html            # Home page
│   │   └── show.html             # Secondary page
│   ├── cert.pem                  # Self-signed SSL certificate (generated)
│   └── key.pem                   # SSL private key (generated)
├── configs/
│   ├── R1_config.txt             # R1 show running-config export
│   └── R2_config.txt             # R2 show running-config export
├── data/
│   ├── traffic_log.csv           # Generated by capture_traffic.py
│   └── performance_results.csv   # Generated by performance_metrics.py
├── charts/
│   ├── performance_comparison.png # Generated by visualize_traffic.py
│   └── traffic_analysis.png       # Generated by visualize_traffic.py
├── requirements.txt
├── .gitignore
├── README.md
├── REFERENCE_PROMPT.md           # This file
└── project_description.md        # Original project guide
```

---

## Python Dependencies (requirements.txt)

```
flask>=2.3
werkzeug>=2.3
pyopenssl>=23.2
netmiko
scapy
matplotlib
pandas
requests
paramiko
urllib3
```

Install: `pip install -r requirements.txt`

---

## Script Specifications

### Script 1: `scripts/ssh_connect.py` — Netmiko SSH

**Purpose:** Connect to Cisco routers via SSH, execute show commands, print and return results.

**Behavior:**
- Connect to R1 at 192.165.10.37 using `netmiko.ConnectHandler` with `device_type='cisco_ios'`
- Credentials: username=`admin`, password=`admin123`, secret=`admin123`
- Call `connection.enable()` to enter privileged exec mode
- Execute these commands and store output in a dict keyed by command string:
  - `show ip interface brief`
  - `show ip route`
  - `show access-lists`
  - `show policy-map interface GigabitEthernet0/0`
- Print each command's output with a header
- Disconnect when done
- `if __name__ == "__main__":` calls the function with R1's IP

---

### Script 2: `scripts/capture_traffic.py` — Scapy Packet Capture

**Purpose:** Capture HTTP and HTTPS packets on the client's network interface for 60 seconds, log to CSV.

**Behavior:**
- Use `scapy.all.sniff()` on interface (detect with `ipconfig`, likely `Ethernet` or `Wi-Fi`)
- Timeout: 60 seconds
- Callback function filters packets that have both TCP and IP layers
- Only record packets where src_port or dst_port is 80 or 443
- Label as `"HTTP"` if port 80 involved, `"HTTPS"` if port 443
- Each entry: `timestamp`, `src_ip`, `dst_ip`, `src_port`, `dst_port`, `protocol`, `length`
- Save to `data/traffic_log.csv` using `csv.DictWriter`
- **Requires Administrator privileges on Windows to run**

**CSV columns:** `timestamp,src_ip,dst_ip,src_port,dst_port,protocol,length`

---

### Script 3: `scripts/performance_metrics.py` — HTTP vs HTTPS Benchmark

**Purpose:** Send 20 requests each to HTTP and HTTPS endpoints, measure and compare performance.

**Behavior:**
- Targets: `http://192.165.20.79` (HTTP) and `https://192.165.20.79` (HTTPS)
- 20 GET requests per target with `requests.get(url, timeout=10, verify=False)`
- Suppress urllib3 InsecureRequestWarning
- Measure per-request elapsed time in ms
- Sleep 0.2s between requests
- Metrics per protocol: `protocol`, `url`, `requests`, `errors`, `avg_ms`, `min_ms`, `max_ms`, `stdev_ms`, `throughput_kbps`, `error_rate_%`
- Save to `data/performance_results.csv`

---

### Script 4: `scripts/visualize_traffic.py` — Matplotlib Charts

**Purpose:** Read CSV data files and generate comparison charts as PNG images.

**Function 1: `plot_response_comparison()`** — reads `data/performance_results.csv`
- 1×3 subplots (15×5): avg response time bars, throughput bars, min/avg/max error bars
- Colors: HTTP=#2196F3, HTTPS=#4CAF50
- Save to `charts/performance_comparison.png` at 150 DPI

**Function 2: `plot_traffic_over_time()`** — reads `data/traffic_log.csv`
- 2×1 subplots (12×8): packet count by protocol, packet size distribution histogram
- Save to `charts/traffic_analysis.png` at 150 DPI

---

### Script 5: `scripts/dashboard.py` — Flask Live Dashboard

**Purpose:** Real-time web dashboard showing HTTP vs HTTPS response times.

**Behavior:**
- Flask on `0.0.0.0:5000`
- Background daemon thread polls both servers every 3s, stores last 100 samples
- `/` serves HTML with Chart.js line chart (dark theme, 2-column grid)
- `/api/metrics` returns JSON: `http_avg_ms`, `https_avg_ms`, `http_samples`, `https_samples`

---

## Server Specifications

### `server/secured_server.py` — Flask HTTPS Server (port 443)

- SSL context using `cert.pem` and `key.pem`
- Security headers: X-Content-Type-Options, X-Frame-Options, X-XSS-Protection, HSTS, CSP
- Request logging to `server.log`
- Path traversal protection (reject `..`)
- Custom error handlers: 403, 404, 500
- Routes: `/` → `index.html`, `/show-something` → `show.html`

### `server/http_server.py` — Flask HTTP Server (port 80)

- Same routes, logging to `http_server.log`
- No SSL, no security headers (plain HTTP for comparison)

### Generate SSL Certificates (on Windows Server)

Use Git Bash, WSL, or OpenSSL for Windows:
```bash
openssl req -x509 -nodes -days 365 -newkey rsa:2048 ^
  -keyout server\key.pem -out server\cert.pem ^
  -subj "/CN=192.165.20.79/O=CCEN356Lab"
```

### Windows Firewall

Open ports 80 and 443 (run as Administrator in PowerShell):
```powershell
New-NetFirewallRule -DisplayName "CCEN356 HTTP" -Direction Inbound -LocalPort 80 -Protocol TCP -Action Allow
New-NetFirewallRule -DisplayName "CCEN356 HTTPS" -Direction Inbound -LocalPort 443 -Protocol TCP -Action Allow
```

---

## Cisco Router Configurations

### R1 Configuration

```
hostname R1
enable secret cisco123
line console 0
 password cisco123
 login

interface GigabitEthernet0/0
 ip address 10.1.5.21 255.255.255.252
 no shutdown
 description WAN_TO_R2

interface GigabitEthernet0/1
 ip address 192.165.10.37 255.255.255.0
 no shutdown
 description LAN_SIDE

ip route 192.165.20.0 255.255.255.0 10.1.5.22

ip domain-name lab.local
crypto key generate rsa modulus 2048
ip ssh version 2
username admin privilege 15 secret admin123
line vty 0 4
 transport input ssh
 login local

! ACL
ip access-list extended HTTP_HTTPS_ONLY
 permit tcp any any eq 80
 permit tcp any any eq 443
 permit tcp any eq 80 any established
 permit tcp any eq 443 any established
 permit icmp any any

interface GigabitEthernet0/0
 ip access-group HTTP_HTTPS_ONLY in

! QoS
class-map match-any HTTP_HTTPS
 match protocol http
 match protocol https

policy-map WEB_QOS
 class HTTP_HTTPS
  priority 1000
 class class-default
  fair-queue

interface GigabitEthernet0/0
 service-policy output WEB_QOS
```

### R2 Configuration

```
hostname R2
enable secret cisco123
line console 0
 password cisco123
 login

interface GigabitEthernet0/0
 ip address 10.1.5.22 255.255.255.252
 no shutdown
 description WAN_TO_R1

interface GigabitEthernet0/1
 ip address 192.165.20.37 255.255.255.0
 no shutdown
 description TO_SERVER

ip route 192.165.10.0 255.255.255.0 10.1.5.21

ip domain-name lab.local
crypto key generate rsa modulus 2048
ip ssh version 2
username admin privilege 15 secret admin123
line vty 0 4
 transport input ssh
 login local
```

### SW1 Configuration

```
hostname SW1
monitor session 1 source interface GigabitEthernet0/1 both
monitor session 1 destination interface GigabitEthernet0/10
```

---

## Execution Order (Lab Day)

1. Cable topology, power on all devices
2. Configure R1, R2 via console cable (PuTTY / minicom)
3. Configure SW1 SPAN port
4. Set up Server PC (Windows): open firewall ports, start Flask HTTP + HTTPS servers
5. Verify: `ping 192.165.20.79` from client PCs
6. Verify: `curl http://192.165.20.79` and `curl -k https://192.165.20.79` (or `Invoke-WebRequest` on Windows)
7. Start Scapy capture on Monitor PC
8. Run `sudo python3 scripts/capture_traffic.py` on Client 1
9. Run `python3 scripts/performance_metrics.py` on Client 2
10. Wait for capture to complete (60s)
11. Run `python3 scripts/visualize_traffic.py` — generates charts
12. Run `python3 scripts/ssh_connect.py` — collects router show outputs
13. Run `python3 scripts/dashboard.py` — screenshot at `http://localhost:5000`
14. Export `show running-config` from R1 and R2 to `configs/`
15. Save the Scapy CSV log and take screenshots

---

## Scapy Analysis Procedure

1. Run `python scripts/capture_traffic.py` on the client that will monitor traffic.
2. Capture during script execution (60+ seconds).
3. Review `data/traffic_log.csv` for HTTP and HTTPS packet entries.
4. Save the CSV plus the generated charts for the report appendix.

---

## Report Structure

| Chapter | Content |
|---|---|
| **Ch 1 — Overview** | HTTP vs HTTPS background, TLS/SSL concepts, project objectives, why QoS and ACLs matter |
| **Ch 2 — Implementation** | Topology diagram, IP addressing table, router/switch config summaries, script purpose and logic (**no code in body**) |
| **Ch 3 — Evaluation** | Scapy capture logs with analysis, matplotlib charts, CSV data tables, QoS impact analysis |
| **Ch 4 — Discussion** | HTTP vs HTTPS overhead findings, QoS effect, team contributions, broader impact |
| **Appendix** | Full `show running-config` from both routers, all Python source code, raw Scapy capture logs |

**Formatting:** Times New Roman 12pt. Source code **only in Appendix**.

---

## Verification Checklist

- [ ] `ping 192.165.20.79` from every client succeeds
- [ ] `ssh admin@192.165.10.37` from client works
- [ ] `curl http://192.165.20.79` returns HTML
- [ ] `curl -k https://192.165.20.79` returns HTML
- [ ] `python3 scripts/ssh_connect.py` prints router show outputs
- [ ] `sudo python3 scripts/capture_traffic.py` → `data/traffic_log.csv`
- [ ] `python3 scripts/performance_metrics.py` → `data/performance_results.csv`
- [ ] `python3 scripts/visualize_traffic.py` → PNGs in `charts/`
- [ ] `python3 scripts/dashboard.py` → live dashboard at `:5000`
- [ ] `show access-lists` shows hit counts
- [ ] `show policy-map interface Gi0/1` shows QoS counters
- [ ] `data/traffic_log.csv` saved with packet entries and analysis screenshots

---

## Key Decisions

- **Physical hardware** (not Packet Tracer) — Cisco 2901 routers, 2960 switch
- **Windows PC** as server (192.165.20.79) — Flask serves HTTP (:80) and HTTPS (:443) directly, no Apache
- **Self-signed SSL** certs — `verify=False` in Python scripts is acceptable
- **Scapy requires root/sudo** to capture packets (on Linux clients)
- **All Python scripts** run on client PCs (Linux)
- **Flask dashboard** (Script 5) is included
- **All code is fresh** — no pre-existing codebase
